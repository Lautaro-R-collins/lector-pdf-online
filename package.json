{
  "name": "lector-pdf-online",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix lector-front",
    "build": "npm run build --prefix lector-front",
    "preview": "npm run preview --prefix lector-front",
    "dev:front": "npm run dev --prefix lector-front",
    "build:front": "npm run build --prefix lector-front",
    "dev:back": "npm run dev --prefix lector-back",
    "start:back": "npm run start --prefix lector-back",
    "test:back": "npm run test --prefix lector-back"
  }
}
